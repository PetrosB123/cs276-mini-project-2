using Unity.VisualScripting;
using UnityEditor.UI;
using UnityEngine;

public class Collectible : ScriptableObject
{
    public virtual void Collect(CollisionHandler collisionHandler)
    {
    
    }
}
